<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Record" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Record" parent="Base.Theme.Record" />

    <!-- 用于可滑动控件中取出进度条及滑动至顶的动画 -->
    <style name="Formit">
        <item name="android:scrollbars">none</item>
        <item name="android:overScrollMode">never</item>
    </style>
    <!-- 适用于Recycler的动画去除 -->
    <style name="Recycler" parent="Formit">
        <item name="android:fadingEdge">vertical</item>
        <item name="android:fadingEdgeLength">10dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
    </style>
</resources>